# Echo — agent notes

Echo is a **Next.js** app (`frontend/`) plus an optional **Node mock API** (`backend/`). npm workspaces at the repo root; see `README.md` for install and run commands.

For implementation work (features, bugs, refactors), follow the **echo-subagent** project skill at `.cursor/skills/echo-subagent/SKILL.md` — it defines the task loop and verification steps used for this codebase.
